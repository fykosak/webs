{varType string $lang}
{varType App\Models\NetteDownloader\ORM\Models\ModelSchedule[] $scheduleGroups}
{varType App\Models\NetteDownloader\ORM\Models\ModelPersonSchedule[][] $personGroups}
{foreach $scheduleGroups as $group}
    <h3>{$group->name[$lang]}</h3>
    <div class="row">
        {foreach $group->scheduleItems as $scheduleItem}
            <div class="col col-md-3">
                <h4>{$scheduleItem->name[$lang]}</h4>
                {if isset($personGroups[$scheduleItem->scheduleItemId])}
                    <ul>
                        {foreach $personGroups[$scheduleItem->scheduleItemId] as $person}
                            <li>{$person->person->name}</li>
                        {/foreach}
                    </ul>
                {/if}
            </div>
        {/foreach}
    </div>
{/foreach}
    {dump $scheduleGroups}
